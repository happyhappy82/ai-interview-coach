/** @type {import('next').NextConfig} */
// Force rebuild: 2026-01-19
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**.supabase.co',
      },
    ],
  },
}

module.exports = nextConfig
